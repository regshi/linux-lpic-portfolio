

## 実行メモ

Macのターミナルから `12-git-cli/git-cli-basic.md` にMarkdownを追記した。

その後、以下のGitコマンドを使ってGitHubへ変更を反映する。

```bash
git status
git add 12-git-cli/git-cli-basic.md
git commit -m "Add Git CLI practice memo"
git push
